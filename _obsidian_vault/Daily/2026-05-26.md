# 2026-05-26

## HermesGuildCore Guild UI Runtime Handoff

### Context

User corrected the earlier progress estimate: the Guild app should not be treated as "80% done" if measured against the intended architecture.

Intended architecture:

- Hermes/Guild Master splits a product into bounded modules.
- Each worker owns one module based on rank/skill/provider capability.
- Workers publish artifacts to the blackboard.
- A join-review / meeting task reads artifacts, detects mismatches, and creates bounded fix tasks for the responsible module/worker.
- Fix tasks run, publish new artifacts, and the meeting repeats until final product or stop condition.
- UI should make this visible; hidden background work without logs is unacceptable.

Current reality before today's fixes:

- UI could create a quest, open worker terminals, call provider adapters, publish artifacts, and show task state.
- Worker tasks were too generic (`required_skill=general`), so workers could claim the wrong module.
- Review/finalize mostly waited for all build tasks to be done; if one failed, the board appeared stuck.
- OpenCode worker calls could fail with Windows `The command line is too long` because the adapter passed the whole prompt through argv.
- Dashboard server was launched hidden without obvious stdout/stderr logs.

### Changes Made Today

Files touched:

- `docs/incubation/guild-dashboard.html`
- `scripts/guild-dashboard-server.py`
- `scripts/guild_provider_adapters/opencode.py`
- `scripts/open-guild-dashboard.ps1`
- `_runtime/flock/worker_team_prototype.py`

Behavior changes:

- Page-level scroll restored in Guild Dashboard UI.
- `general` is no longer a wildcard skill in `skill_can_claim`; a non-general task now requires the exact skill.
- Manual/Hermes plan task generation now uses module-specific skills:
  - build-1 -> `requirements`
  - build-2 -> `risk-analysis`
  - build-3 -> `verification`
- Worker profiles already map naturally:
  - worker-a has `requirements`
  - worker-b has `risk-analysis`
  - worker-c has `verification`
- `/api/hermes/finalize` now acts as a basic artifact meeting/repair gate:
  - If build modules are failed or missing expected files, it creates fix tasks with the right module skill.
  - Review task waits on those fix tasks.
  - Once modules are present/done, finalize can write `review.md` and `final-summary.md`.
- OpenCode adapter now writes long prompts to `_runtime/guild-provider-adapters/opencode-prompts/` and calls OpenCode with `--file`, avoiding Windows command-line length errors.
- Dashboard launcher now exposes hidden server logs:
  - `_runtime/dashboard/guild-dashboard-server.out.log`
  - `_runtime/dashboard/guild-dashboard-server.err.log`

### Verified Evidence

- Direct OpenCode smoke passed outside Codex sandbox after dot-sourcing `_runtime/provider-secrets.local.ps1`.
- OpenRouter and Groq workers had already produced successful artifacts in the test quest.
- Existing broken quest `quest-manual-dashboard-task-20260526012155` was used to test repair behavior.
- `/api/hermes/finalize` created fix tasks:
  - `manual-dashboard-task-20260526012155-build-1-fix-1` with skill `requirements`
  - `manual-dashboard-task-20260526012155-build-2-fix-1` with skill `risk-analysis`
  - `manual-dashboard-task-20260526012155-build-3-fix-1` with skill `verification`
- Smoke quest `quest-module-binding-smoke-20260526` showed new task generation uses skill-specific modules.

### Important Caveats

- This is still a pragmatic v1 bridge, not the final scheduler.
- There is still hard-coded module routing for three tracks in `guild-dashboard-server.py`.
- Future work should reduce hard-coding by moving worker capability, rank policy, module templates, and claim policy into `config/guild`.
- The current UI button label "Giao Task Cho Hermes" calls local dashboard API `/api/hermes/quest`, not the Hermes gateway. It uses Hermes CLI planner logic, so gateway may not visibly react.
- Do not expose provider key values. `_runtime/provider-secrets.local.ps1` is the ignored local key loader.
- Codex sandbox may show `uv_spawn 'git'` for OpenCode; test OpenCode in normal terminal when checking provider health.

## HermesGuildCore Guild Runtime Follow-up

Completed the hard-code reduction sequence for the Guild UI runtime.

Changes:

- Added `config/guild/guild-runtime.json` for module tracks, scheduler policy, review policy, provider adapter preferences, and final assembly files.
- `scripts/guild-dashboard-server.py` now reads runtime config for module task generation, worker wake profiles, auto-rank adapter distribution, fix ownership, expected output files, and final assembly validation.
- Scheduler selection now derives worker profiles from configured agent rank/skills instead of assuming fixed `worker-a/b/c`.
- Join-review/finalize repair logic creates fix tasks only for failed or missing modules. Targeted smoke created only `build-2-fix-1` when module 2 failed.
- Dashboard UI now includes a derived Guild Event Log and Meeting Rounds panel so planning, claims, artifacts, fixes, and final assembly are visible.
- Final assembly now writes `review.md`, `final-summary.md`, and durable `final-artifact.json`, then validates configured module/final files.
- Reviewer profile now includes `integration_review` so it can claim join-review tasks when that path is used.

Verified:

- Syntax compile check passed via `compile(...)`; direct `py_compile` is blocked by Windows access denial on `scripts/__pycache__`.
- API scheduler smoke on port `8791` created 5 tasks and selected `worker-a,worker-b,worker-c` from config, with auto-rank adapters `opencode,openrouter,groq`.
- Targeted meeting smoke on port `8793` created only one fix task for failed module 2 and then finalized successfully with `final-artifact.json`.
- Clean provider smoke passed outside sandbox for `opencode`, `openrouter`, and `groq`. Sandbox blocks provider/network paths with local refusal/provider failure, so provider health should be checked outside sandbox or through the dashboard Provider Lab.

### Afternoon Resume / Current Remaining Issues

Read first:

- `START_HERE.md`
- `TASKS.md`
- `config/guild/guild-runtime.json`
- `docs/core/HERMES_RUNTIME_ROUTER.md`
- `docs/workers/PROVIDER_ADAPTERS.md`
- `scripts/guild-dashboard-server.py`
- `docs/incubation/guild-dashboard.html`
- `_runtime/flock/worker_team_prototype.py`

Continue from the remaining issues below, not from the old hard-code checklist.

Remaining issues:

1. Run one visible end-to-end dashboard smoke with real worker terminals.
   - Use the config scheduler path.
   - Confirm UI logs show planning, launch, claim, artifact, meeting/fix, and final assembly live.
2. Decide finalization ownership.
   - Current state: `/api/hermes/finalize` owns meeting/final assembly.
   - Open question: should final assembly move into a normal `join_review` worker claim path for a cleaner blackboard model?
3. UI event log is derived, not durable.
   - It reads current tasks/artifacts and synthesizes events.
   - If auditability matters, add a durable event table or event artifact stream later.
4. Scheduler policy is v1.
   - Done: rank threshold and exact skill match through config.
   - Not done: weighted skill matching, provider quota/health checks, retry ownership policy, explicit owner pinning.
5. Provider health depends on running outside sandbox.
   - Clean smoke passed outside sandbox for `opencode`, `openrouter`, and `groq`.
   - In Codex sandbox, provider/network paths can fail with refused connections or OpenCode sandbox quirks.
   - Use normal terminal or Provider Lab for provider checks.
6. PowerShell profile noise remains.
   - `fnm env --use-on-cd` can print `fnm_multishells` access denied / `Invoke-Expression` empty command errors.
   - It does not block Guild runtime, but it pollutes command output and should be fixed separately.
7. Git worktree is dirty.
   - Some files were already dirty before this work.
   - Review `git diff` carefully before committing; do not bundle unrelated prior edits accidentally.
8. Legacy scheduled jobs may still reference `D:\TuanKeCuoi`.
   - Treat as path-sensitive/guarded work.
   - Use dangerous-operation guard before changing cron/task paths.
9. `n8n` boundary remains.
   - Keep n8n as Daily Team runtime, not Worker Team runtime.
   - This was not changed in the Guild UI runtime work.

### Architecture Clarification For Afternoon

Decision:

- Providers do not need to "enter Guild" or understand the whole Guild runtime.
- The correct contract is: Guild creates bounded tasks, worker wrapper claims tasks, provider performs only the assigned task packet, worker wrapper validates output, then publishes artifact back to Guild.
- Provider sandbox/network quirks are environment problems, not the core architecture criterion.

Current direction is right:

- Dashboard/user gives task to Hermes/Guild.
- Guild/Hermes creates task contracts and allowed scope.
- Workers claim from blackboard.
- Providers are called through adapters.
- Output validation prevents bad provider output from being marked done.

Important correction:

- Current `/api/hermes/finalize` works as a pragmatic bridge, but it is still API-owned.
- Cleaner Guild model: finalization/meeting should become a normal `join_review` or `final_review` task that is claimed, executed, validated, and published through the same worker-wrapper/artifact path.
- Afternoon priority should be this correction before adding more scheduler complexity, unless visible smoke exposes a more urgent bug.

### GPT-5.4 Work Log

This block was done by GPT-5.4 and should be checked again this afternoon.

Work completed:

- Added `config/guild/guild-runtime.json` and used it for module tracks, scheduler policy, review policy, and final assembly file targets.
- Updated `scripts/guild-dashboard-server.py` so planner/task generation and final assembly follow config-driven module tracks instead of fixed hard-coded branches.
- Updated the dashboard UI to show derived Guild Event Log and Meeting Rounds.
- Added runtime enforcement so Hermes planner output must match the expected JSON/track shape.
- Added runtime enforcement so worker artifacts cannot declare `files_changed` outside `allowed_files`.
- Added PowerShell shortcuts for `ff`, `fg`, `pv`, and `lsw`, and suppressed `fnm` startup noise in the active profile.

Needs recheck this afternoon:

- Run a visible end-to-end dashboard smoke with real worker terminals.
- Verify the new runtime gates still work after a fresh shell reload.
- Confirm the `finalize` path is still acceptable, or move it to a normal `join_review` / `final_review` task path if needed.

Verification update:

- `fg` and `ff` were reloaded successfully from `$PROFILE` and returned the expected results.
- The PowerShell profile still emits one reload-time error: `Set-Alias cat bat` conflicts with an existing `AllScope` alias for `cat`.
- `fnm` startup noise did not show up in this verification pass.

### Deep-Dive Insight

This is the current judgment on whether the system can be taken deeper without cooking it:

- The Guild runtime is stable enough to go deeper on the blackboard/task/artifact path.
- The safe direction is incremental hardening, not a large rewrite.
- Strongest current pieces:
  - task/artifact blackboard shape
  - claim/lease/heartbeat
  - `allowed_files` enforcement
  - adapter output validation
  - config-driven module tracks
- Weakest current pieces:
  - `finalize` is still API-owned bridge logic
  - event log is derived, not durable
  - scheduler is only v1
  - provider/network health still depends on environment
- Practical takeaway:
  - continue only in small slices
  - run a smoke after each slice
  - stop and re-evaluate if a change needs a bigger architectural shift

Best next sequence if continuing:

1. Run a visible end-to-end dashboard smoke with real worker terminals.
2. Decide whether `finalize` stays API-owned or becomes a normal `join_review` / `final_review` task path.
3. Add durable event/audit storage only if the derived log becomes insufficient.
4. Expand scheduler policy only after the current flow stays stable under smoke.

### Book Insight

We are not forcing Guild to follow the book as doctrine. We only record useful ideas and keep the ones that fit the system.

From the blackboard / multi-agent flow analysis in the book-like material:

- The useful pattern is not "many agents talking", but "shared state + specialists + orchestrator".
- Specialists should wake only when the shared state makes them relevant, not by constant polling.
- Triggering should be event-driven and bounded, otherwise the system turns into noisy scanning.
- Concurrency can work if shared state is narrow and contention is controlled at the state layer, not by letting agents free-run.
- Data and control should be separated: agents reason over state, but the runtime owns scheduling and termination.

What this means for Guild:

- Keep blackboard/task/artifact as the center of gravity.
- Keep workers narrow and role-bound instead of general-purpose.
- Prefer runtime-enforced triggers and validations over prompt-only instructions.
- Do not let Hermes/worker behavior rely on "hope the model follows the rule"; encode the rule in launcher/wrapper/runtime checks.

Main warning from the book insight:

- If the system only has a shared board but no disciplined triggering/termination, it will drift into scan-noise and ambiguous completion.
- So the next layer should be more enforcement and audit, not more agent freedom.

### Afternoon Codex Recheck And Cleanup

Scope:

- Re-read this daily note before acting.
- Rechecked GPT-5.4 morning work instead of immediately adding new Guild features.
- Focused on small hardening fixes, bounded smoke tests, and cleaning the git worktree.

Findings and fixes:

- `/api/hermes/finalize` had a false-complete path: if `join_review` was already `done`, it returned `already_done=true` before validating module/final files.
- Fixed finalize so it validates/repairs first and only returns `already_done=true` when `review.md`, `final-summary.md`, and `final-artifact.json` exist and validate.
- Found repeated runtime dependence on literal `powershell` in environments where `powershell` is not on PATH.
- Added PowerShell executable resolution/fallbacks for dashboard export, `/api/wake`, Provider Lab test, Telegram finalize report, visible worker launch, and visible opencode worker launch.
- Removed stale runtime dependency from `start-guild-worker-terminal.ps1` on `tick-guild-dashboard.ps1`; the current UI-first path uses `run-guild-worker-agent.ps1`.
- Kept v0 names/contracts; did not delete v0 files because most are schema/bootstrap contracts, not obsolete UI code.

Verification:

- `compile(...)` passed for `scripts/guild-dashboard-server.py`.
- Local dry-run Guild smoke created `quest-codex-local-smoke-20260526`, ran `worker-a`, `worker-b`, `worker-c`, and `reviewer`, and reached `done=5` with 4 artifacts.
- Patched finalize then created targeted fix tasks when expected module files were missing instead of falsely accepting completion.
- Planner validation accepted a valid 3-track plan and rejected a bad 2-track plan.
- Fresh shell check loaded `ff`, `fg`, `pv`, and `lsw`; no `fnm` startup noise appeared in that check.
- Provider Lab deterministic smoke passed with `local:dry-run`.
- `/api/wake` dry-run passed after PowerShell resolver patch.
- `start-guild-worker-terminal.ps1 -DryRun` passed.
- `git diff --check` passed.
- Dashboard test servers started on ports `8795-8798` were inspected by exact command line and stopped.

Commits created locally:

- `cb4d7df Add Hermes runtime routing bootstrap`
- `d12183d Harden Guild dashboard runtime launch paths`
- `49fa755 Validate Guild worker output scope`
- `30c974c Record Guild runtime recheck notes`

Remaining open items:

- Hermes gateway is currently not running; cron jobs will not fire automatically until gateway is started. Starting gateway is an operator action and needs explicit approval.

### Visible Smoke Cleared

Cleared the first remaining item: visible end-to-end dashboard smoke with real worker terminals.

What happened:

- Started a local dashboard server and created `quest-visible-clear-20260526`.
- `/api/wake` launched real visible terminals for `worker-a`, `worker-b`, and `worker-c`.
- Build workers completed and published artifacts, but review stayed `open` because wake profiles did not include `reviewer`.
- Direct reviewer agent claim worked, proving the blackboard/claim path was healthy and the gap was wake-profile scheduling.

Fix:

- Updated `scripts/guild-dashboard-server.py` so quest wake profiles include a review-capable profile after build workers.
- Kept build scheduler exclusion of `reviewer`; reviewer is only added to full quest wake, not build-track selection.

Verification:

- New quest `quest-visible-clear-autoreview-20260526` returned wake profiles `worker-a`, `worker-b`, `worker-c`, and `reviewer`.
- `/api/wake` launched all four visible worker terminals with `local-dry-run`.
- Dashboard reached `done=5` and `artifact_count=4`.
- Artifacts were published by `worker-a`, `worker-b`, `worker-c`, and `reviewer`.
- Smoke server and visible worker processes were inspected by exact command line and stopped afterward.

### Finalization Ownership Decision

Cleared the finalization ownership question for the current v1 bridge.

Decision:

- Keep `/api/hermes/finalize` as the v1 final assembly owner for now.
- Treat API-owned finalization as an explicit bridge, not the final Guild architecture.
- Target v2 should move final assembly into a normal `final_review` or stronger `join_review` worker task path after worker file-writing semantics are stable.

Reasoning:

- The patched finalize path now validates module/final files and creates targeted fix tasks instead of falsely accepting completion.
- The visible smoke proved normal `join_review` claim/artifact publication works, but `local-dry-run` does not write actual final files.
- Moving final assembly into worker-wrapper execution now would be a larger architectural slice and would mix file-writing semantics, review semantics, and final artifact validation in one step.
- Keeping the bridge lets the current UI demo remain stable while still exposing the future migration target clearly.

Required guardrail:

- Do not describe `/api/hermes/finalize` as the final Guild model.
- Label it as `hermes_finalize_v1` / API bridge behavior in logs/docs.
- Revisit migration only after a real worker can reliably write scoped files, pass `allowed_files` validation, and publish final artifacts without hidden side effects.

### Remaining Item Triage

Cleared the remaining ambiguity list into decisions or approval-gated follow-up.

Event log:

- Decision for v1: keep the Guild Event Log derived from tasks/artifacts.
- Do not add a durable event table yet.
- Reason: current artifact/task state is enough for the v1 UI demo, and durable event storage would be a new schema/migration slice.
- Revisit when auditability becomes a real product requirement rather than a demo visibility requirement.

Scheduler:

- Decision for v1: keep the scheduler simple: rank threshold + exact skill + configured adapter preferences.
- Do not add weighted skill matching, quota/health checks, retry ownership policy, or explicit owner pinning in this cleanup pass.
- Reason: visible smoke now passes end-to-end; expanding scheduler policy before real provider runs would add complexity without fresh evidence.

Provider health:

- Current rule stands: test real providers outside Codex sandbox or through Provider Lab.
- Provider Lab deterministic smoke already passed with `local:dry-run`.
- Earlier same-day note recorded clean provider smoke outside sandbox for `opencode`, `openrouter`, and `groq`.
- No provider secrets were exposed or committed.

Legacy scheduled jobs:

- Ran `hermes cron list`.
- Active jobs now use `Workdir: D:\HermesGuildCore`; no active Hermes cron job showed `D:\TuanKeCuoi`.
- Jobs checked: `daily-morning-brief`, `game-checkin-21`, `game-checkin-22`, `hermes-healthcheck`.
- Separate operational finding: Hermes gateway is not running, so scheduled jobs will not fire automatically until gateway is started.
- Starting or changing gateway/cron remains approval-gated operator work.

### Session Close

- Repo was clean and pushed after clearing the 2026-05-26 Guild runtime cleanup items.
- No machine restart is required.
- Remaining operational item: Hermes gateway is not running; start/check gateway only with explicit user approval.

### Afternoon Full-Deployment Gap Work

User corrected the framing again: today's cleanup is not "done"; GPT-5.4's estimate that the system is roughly 40% toward the intended full Guild deployment is still the right baseline.

The next gap addressed was worker file-writing semantics. The v1 bridge can finalize, but `local-dry-run` only publishes artifacts and does not prove that workers can create scoped files for final assembly.

Implemented:

- Added deterministic provider adapter `local-file-writer`.
- Added the adapter to `scripts/guild_provider_adapters/registry.py`.
- Added the adapter config entry in `config/guild/provider-adapters.json`.
- Added the adapter to the dashboard Provider dropdown in `docs/incubation/guild-dashboard.html`.
- The adapter parses the current `GuildTask` packet, infers the expected output file from the task request or artifact naming, writes under `guild-workspaces/<quest_chain_id>/`, validates the write against `allowed_files`, and returns normal artifact JSON.

Smoke results:

- First visible smoke `quest-file-writer-smoke-20260526` failed safely.
- Failure reason: task JSON had `allowed_files` as an array, while the new adapter initially treated it like a string and blocked its own scoped output as `files_outside_allowed_scope`.
- Fixed `allowed_files` normalization to accept both list and string forms.
- Second visible smoke `quest-file-writer-smoke-v2-20260526` passed with four visible worker terminals.
- Dashboard reached `done=5`, `artifact_count=4` before finalization.
- Worker-created files existed:
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-1.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-2.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-3.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/review.md`
- `/api/hermes/finalize` then wrote and validated:
  - `final-summary.md`
  - `final-artifact.json`
  - final `review.md`
- Final dashboard had `artifact_count=5`, including the `hermes-codex` finalization artifact.
- Smoke server and worker terminal processes were inspected by exact command line and stopped afterward.

Current interpretation:

- This closes one real gap between demo artifacts and actual file-backed module work.
- It is still not the final architecture.
- Remaining full-deployment work is to make real provider-backed workers reliably write scoped files, then migrate final assembly from API-owned `hermes_finalize_v1` into a normal worker-claimed `final_review` or stronger `join_review` task path.

### End-of-Day Plain Summary

Plain explanation for a non-coder:

- Hermes/Guild is being built like a small company of AI workers.
- The user gives one big request.
- Hermes should split that request into smaller jobs.
- Each worker takes one job, produces visible output, and posts proof back to a shared board.
- A reviewer checks whether the pieces fit together.
- If something is missing or wrong, Hermes should create a small fix job instead of pretending the whole thing is done.
- The final product should only be marked complete after the required files exist and pass validation.

What was corrected today:

- The system should not be described as nearly finished.
- It is closer to a working bridge/demo than a full autonomous Guild.
- GPT-5.4's rough "about 40%" framing is still the safer mental model.

What was completed today:

- Rechecked the morning work and confirmed the old false-complete bug in finalization was fixed.
- Confirmed worker tasks are now split by skill instead of letting any generic worker claim anything.
- Confirmed the visible dashboard can launch worker terminals and show work moving through the board.
- Fixed the wake flow so reviewer workers are launched after build workers.
- Kept finalization as a temporary API bridge, but documented clearly that it is not the final architecture.
- Added a new deterministic `local-file-writer` worker adapter.
- Proved workers can now write real scoped files, not only fake/dry-run artifacts.
- Ran a failed smoke first, found the cause, fixed it, then ran a passing smoke.
- Verified final files were produced and validated.
- Stopped leftover test processes.
- Committed and pushed all changes.

Why this matters:

- Before this, a worker could say "I made something" by publishing an artifact, but the test did not prove that real files were created.
- Now there is a deterministic baseline proving the file-writing contract:
  - worker receives a bounded job
  - worker writes only inside the allowed folder
  - worker reports the changed file
  - finalization validates the expected files
- This is a necessary step before trusting real model providers to write project files.

Current safe state:

- Git worktree was clean after push.
- Smoke processes were cleaned up.
- No machine restart is needed.
- Hermes gateway is still not running; scheduled jobs will not fire until the gateway is started.
- Starting/checking gateway remains an explicit operator action, not an automatic next step.
- Provider secrets were not exposed or committed.

Plan for next session:

1. Start with a read-only status check: git status, latest commit, and this note.
2. Do not add scheduler complexity first.
3. Use `local-file-writer` as the baseline contract for worker file output.
4. Make real provider-backed workers follow the same scoped file-writing contract.
5. Only after real provider file writes are stable, move final assembly from the API bridge into a normal worker-claimed `final_review` or stronger `join_review` path.
6. Add durable event/audit storage later only if the derived dashboard log becomes insufficient.
7. Start Hermes gateway only if the user explicitly asks.

Stop point:

- This is a good stopping point for the day.
- Continuing immediately would mix too many concepts at once: real provider file writes, final-review worker migration, and gateway/runtime operation.
- The next session should continue in one small slice, not a broad rewrite.

### Ban Ghi Chep Dai Cho Nguoi Khong Code

Muc tieu lon cua ngay hom nay:

- Khong phai la "lam them mot chut UI".
- Muc tieu that la kiem tra xem Hermes/Guild da tien gan den mo hinh "mot cong ty AI nho" chua.
- Trong mo hinh do, Hermes dong vai tro nguoi quan ly: nhan mot yeu cau lon, chia thanh viec nho, giao cho worker phu hop, nhan ket qua, cho reviewer kiem tra, neu sai thi tao viec sua, neu du thi moi dong goi san pham cuoi.
- Diem quan trong nhat: he thong khong duoc noi "xong roi" neu file that chua ton tai hoac chua duoc validate.

Tinh trang luc bat dau:

- Truoc do da co dashboard, worker, provider adapter, artifact, blackboard, va mot duong finalize.
- Nhung nhieu phan van o muc "cau noi v1", chua phai kien truc Guild day du.
- GPT-5.4 da canh bao dung: neu do theo kien truc mong muon thi khong nen noi la 80%; nen coi la khoang 40%.
- Ly do: co khung chay, co smoke, co artifact, nhung vong lap day du "chia module -> worker lam -> reviewer hop -> tao fix task -> lap lai -> final product" van chua hoan chinh.

Buoc 1: Doc lai memory va xac dinh viec that su can lam

- Doc note ngay 2026-05-26, TASKS, va cac file runtime lien quan.
- Ket luan: khong nen tiep tuc lam UI cho dep hay them tinh nang moi.
- Viec can lam la recheck cac loi nguy hiem trong luong Guild:
  - finalization co bi bao xong gia khong
  - worker co claim dung viec khong
  - dashboard co hien du log khong
  - worker co that su duoc launch khong
  - co can xoa v0 khong
  - worktree co ban khong

Buoc 2: Recheck bug "false complete" trong finalize

- Van de bang ngon ngu don gian:
  - He thong co the bao "da xong" chi vi task review da done.
  - Nhung thuc te cac file cuoi nhu `review.md`, `final-summary.md`, `final-artifact.json` co the chua ton tai.
  - Neu de vay, dashboard trong nhu thanh cong nhung san pham that lai rong hoac thieu.
- Cach xu ly:
  - Sua luong `/api/hermes/finalize`.
  - Truoc khi tra ve "already done", finalize phai kiem tra file that.
  - Neu build module thieu hoac fail, tao fix task dung module thay vi gia bo xong.
  - Chi chap nhan done khi cac file bat buoc ton tai va validation pass.
- Ket qua:
  - Loi "xong gia" duoc chan.
  - Finalize gio la cau noi v1 an toan hon, nhung van duoc ghi ro la chua phai model cuoi.

Buoc 3: Recheck worker co nhan dung viec khong

- Van de bang ngon ngu don gian:
  - Neu moi task deu la `general`, bat ky worker nao cung co the nhan viec.
  - Nhu vay worker chuyen doc yeu cau co the nhan viec test, worker review co the nhan viec build, hoac nguoc lai.
  - Mo hinh "cong ty AI" se hong vi khong ai dung vai.
- Cach xu ly:
  - Khong cho `general` la wildcard de claim moi viec nua.
  - Module duoc gan skill ro:
    - `requirements`
    - `risk-analysis`
    - `verification`
  - Worker profile phai co skill phu hop moi claim duoc task.
- Ket qua:
  - Worker-a, worker-b, worker-c duoc map theo module ro hon.
  - Dashboard/task queue bat dau giong "chia viec cho dung nguoi" thay vi "ai cung lam duoc".

Buoc 4: Giam hard-code bang config

- Van de bang ngon ngu don gian:
  - Neu logic chia module, chon worker, chon adapter bi viet cung trong code, moi lan doi quy trinh phai sua code.
  - He thong nhu vay kho mo rong va kho day cho worker moi.
- Cach xu ly:
  - Them va dung `config/guild/guild-runtime.json`.
  - Dua cac thong tin nhu module tracks, scheduler policy, review policy, final assembly files vao config.
  - Dashboard server doc config de tao task va chon worker.
- Ket qua:
  - Bot phu thuoc vao worker-a/b/c hard-code.
  - Scheduler van la v1 don gian, nhung da co khung de sau nay nang cap.

Buoc 5: Sua loi PowerShell/runtime path

- Van de bang ngon ngu don gian:
  - Mot so script goi `powershell` theo cach co the sai tren may Windows hien tai.
  - Neu runtime goi sai shell, dashboard mo len nhung worker khong chay hoac log kho debug.
- Cach xu ly:
  - Them resolver/fallback cho PowerShell trong cac script launch/runtime.
  - Dam bao dashboard server co log stdout/stderr trong `_runtime/dashboard/`.
- Ket qua:
  - Launch path on dinh hon.
  - Khi loi, co log de doc thay vi hidden background.

Buoc 6: Kiem tra UI visible smoke lan dau

- Muc tieu:
  - Khong chi test API ngam.
  - Phai mo worker terminal that de xem luong "Hermes giao viec -> worker claim -> worker xong -> reviewer review" co chay khong.
- Ket qua lan dau:
  - Dashboard tao quest duoc.
  - `/api/wake` mo worker-a, worker-b, worker-c.
  - Ba build worker lam xong va publish artifact.
  - Nhung review task van open.
- Dau hieu bug:
  - Build done het, nhung reviewer khong chay.
  - Dashboard khong ket thuc duoc vi review bi bo lai.
- Nguyen nhan:
  - Wake profiles chi launch build workers.
  - Reviewer khong nam trong danh sach worker duoc wake cho quest day du.
- Cach xu ly:
  - Sua `scripts/guild-dashboard-server.py`.
  - Them logic de full quest wake co ca review-capable profile sau build workers.
  - Van giu reviewer khong tham gia build-track selection, de reviewer khong tranh viec build.
- Ket qua verify:
  - Quest moi launch du 4 terminal: worker-a, worker-b, worker-c, reviewer.
  - Dashboard dat `done=5`.
  - Artifact count dat 4 truoc finalization.
  - Artifacts co du tu 3 worker build va reviewer.

Buoc 7: Quyet dinh ve finalization ownership

- Cau hoi:
  - Co nen chuyen finalization thanh worker task ngay khong?
  - Hay tam giu `/api/hermes/finalize` lam cau noi v1?
- Quyet dinh:
  - Tam giu `/api/hermes/finalize`.
  - Ghi ro no la `hermes_finalize_v1`, tuc cau noi tam thoi.
  - Khong duoc goi no la kien truc Guild cuoi.
- Ly do:
  - Neu doi sang worker final-review ngay, se phai giai quyet cung luc:
    - worker ghi file that
    - reviewer doc cac file
    - final artifact validation
    - allowed scope
    - retry/fix loop
  - Lam tat ca mot luc se tang rui ro va kho debug.
- Ket qua:
  - V1 bridge duoc giu on dinh.
  - Huong v2 duoc ghi ro: sau khi worker ghi file on dinh, finalization moi chuyen thanh worker-claimed `final_review` hoac `join_review`.

Buoc 8: Kiem tra cac viec con lai va don nha

- Event log:
  - Quyet dinh giu derived event log cho v1.
  - Chua them durable event table vi chua can cho demo/debug hien tai.
- Scheduler:
  - Giu don gian: rank threshold + exact skill + adapter preference.
  - Chua them weighted matching, quota, owner pinning, retry policy.
- Provider health:
  - Nho lai quy tac: test provider that ngoai Codex sandbox hoac qua Provider Lab.
  - Khong expose key.
- Legacy cron:
  - Kiem tra `hermes cron list`.
  - Cac active jobs dung `D:\HermesGuildCore`.
  - Khong thay active job dung `D:\TuanKeCuoi`.
  - Phat hien Hermes gateway chua chay, nen cron se khong tu fire.
  - Khong start gateway vi day la operator action can approval.
- Worktree:
  - Commit/push cac thay doi theo tung commit nho.
  - Dam bao khong de repo ban.

Buoc 9: User nhac lai rang moi 40%, khong duoc nghi som

- Day la diem quan trong trong phien.
- Sau khi cleanup xong, co luc trang thai trong nhu "done".
- User nhac: GPT-5.4 noi moi khoang 40% de full deployment.
- Ket luan moi:
  - Cleanup khong phai ket thuc.
  - Can lam them mot lat cat that su gan hon voi full deployment.
  - Lat cat duoc chon: worker phai ghi file that trong scope.

Buoc 10: Them `local-file-writer`

- Van de bang ngon ngu don gian:
  - `local-dry-run` chi chung minh worker co the noi "toi da lam".
  - No khong chung minh worker co the tao file that.
  - Muon tin real provider sau nay, truoc het can mot adapter cuc ky don gian nhung ghi file that, khong ton tien model.
- Cach lam:
  - Tao `scripts/guild_provider_adapters/local_file_writer.py`.
  - Dang ky adapter trong registry.
  - Them vao `config/guild/provider-adapters.json`.
  - Them option `local-file-writer` vao dashboard dropdown.
- Adapter nay lam gi:
  - Doc packet task hien tai.
  - Tim quest id.
  - Tim output file can ghi, vi du `build-1.md` hoac `review.md`.
  - Ghi file vao `guild-workspaces/<quest_chain_id>/`.
  - Kiem tra file do co nam trong `allowed_files` khong.
  - Tra ve artifact JSON dung contract.
- No khong dung AI model.
- No la thiet bi do chuan: neu adapter don gian nay con fail thi dung goi real provider.

Buoc 11: Smoke `local-file-writer` lan 1 va bug gap phai

- Quest: `quest-file-writer-smoke-20260526`.
- Ket qua:
  - Worker duoc launch.
  - Nhung build tasks fail.
- Dau hieu:
  - Artifact bao `blocked_reason=files_outside_allowed_scope`.
  - Tuc adapter tu chan chinh no, vi nghi file sap ghi nam ngoai vung cho phep.
- Dieu tra:
  - Doc payload artifact trong SQLite.
  - Thay `allowed_files` trong task JSON la array/list:
    - `["guild-workspaces/quest-file-writer-smoke-20260526/**"]`
  - Adapter ban dau lai xu ly no nhu string.
  - Khi list bi stringify, pattern khong match duong dan thuc.
- Nguyen nhan:
  - Sai assumption ve shape cua `allowed_files`.
  - Code tuong `allowed_files` la string, thuc te co luc la list.
- Cach sua:
  - Them `normalize_allowed_files(value)`.
  - Neu value la list, join cac item thanh chuoi pattern.
  - Neu value la string, giu nhu cu.
  - Sau do `is_allowed(...)` moi so khop duong dan.
- Bai hoc:
  - Bug nay tot vi no fail safe.
  - He thong khong ghi file lung tung khi scope khong match.
  - Phai sua parser, khong duoc tat validation.

Buoc 12: Smoke `local-file-writer` lan 2 thanh cong

- Quest: `quest-file-writer-smoke-v2-20260526`.
- Worker terminals:
  - worker-a
  - worker-b
  - worker-c
  - reviewer
- Ket qua dashboard truoc finalization:
  - `done=5`
  - `artifact_count=4`
- File worker tao ra:
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-1.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-2.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/build-3.md`
  - `guild-workspaces/quest-file-writer-smoke-v2-20260526/review.md`
- Y nghia:
  - Khong con chi la artifact noi mieng.
  - Worker da ghi file that trong folder duoc phep.
  - Reviewer cung tao output file.

Buoc 13: Finalize sau khi co file that

- Goi `/api/hermes/finalize`.
- Ket qua:
  - API tra `ok=true`.
  - `ready=true`.
  - Final files duoc tao/validate:
    - `review.md`
    - `final-summary.md`
    - `final-artifact.json`
- Dashboard sau finalization:
  - `artifact_count=5`
  - Co artifact finalization cua `hermes-codex`.
- Y nghia:
  - Lan nay finalization khong chi dua vao task status.
  - No co file-backed evidence.

Buoc 14: Don process sau smoke

- Van de:
  - Visible smoke mo terminal va server test.
  - Neu de lai, may co process rac va port co the bi chiem.
- Cach lam:
  - Inspect process bang command line cu the.
  - Chi stop dung PID cua server/worker lien quan quest smoke.
  - Verify lai process list.
- Ket qua:
  - Smoke server va worker terminals duoc dung.
  - Process con lai luc verify chi la chinh lenh verify.

Buoc 15: Ghi memory, update TASKS, commit, push

- Ghi vao Daily:
  - Cac bug da gap.
  - Cach sua.
  - Ket qua smoke.
  - Trang thai chua full deployment.
- Update TASKS:
  - Next action khong con la visible smoke nua vi da xong.
  - Next action moi:
    - lam real provider-backed workers ghi scoped files
    - sau do chuyen finalization thanh worker-claimed final review
    - durable event/audit de sau
    - scheduler complexity de sau
    - gateway chi start khi user approve
- Commit/push:
  - `e7a38e3 Add deterministic Guild file writer adapter`
  - `eb8e401 Record Guild end of day plain summary`
- Worktree cuoi:
  - clean.

Ket qua cuoi ngay:

- He thong da tien them mot nac that, khong chi don dep.
- Truoc do:
  - Guild co the tao task va artifact.
  - Nhưng file-backed output chua duoc chung minh ro.
- Sau hom nay:
  - Guild co the chia task.
  - Worker visible co the claim va publish.
  - Reviewer co the duoc wake dung.
  - Finalize khong con de dang bao xong gia.
  - Adapter deterministic co the ghi file that trong scope.
  - Final assembly co the validate file that.
- Tuy vay van chua full:
  - Real providers chua duoc ep theo file-writing contract nay trong smoke moi.
  - Finalization van la API bridge, chua phai worker-claimed final review.
  - Event log van derived, chua durable audit stream.
  - Scheduler van v1, chua co quota/health/owner pinning/retry policy day du.
  - Gateway chua chay nen cron khong tu fire.

Ban dich cuc ngan:

- Hom nay ta bien "AI workers noi da lam" thanh "AI worker baseline co the ghi file that va bi kiem soat pham vi".
- Gap bug reviewer khong duoc wake: da sua.
- Gap bug adapter tu chan file hop le vi parse `allowed_files` sai: da sua.
- Gap rui ro finalize bao xong gia: da chan bang validation/fix-task logic.
- Ket qua: repo sach, da push, co smoke pass, co memory, co plan tiep theo.

Plan lan sau cho intern de theo kip:

1. Chi doc note nay va `TASKS.md` truoc.
2. Khong sua scheduler.
3. Khong start gateway neu chua hoi.
4. Lay `local-file-writer` lam mau dung.
5. Bat real provider viet output theo dung mau do.
6. Neu real provider ghi file on dinh, moi tinh chuyen finalization thanh worker task.
7. Moi buoc chi lam mot smoke nho, fail thi ghi bug va sua dung nguyen nhan.
